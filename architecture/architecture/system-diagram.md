# System Architecture

The TrabaHome system uses a layered architecture:

Client (React Web App)
→ Backend API (Node.js + Express)
→ Database (MongoDB)

External services:
- AI Verification (OpenCV / TensorFlow)
- Blockchain Audit Logging (Hyperledger Fabric)
- Google Maps API
- SMS Gateway
